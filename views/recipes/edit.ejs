<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Recipe</title>
</head>
<body>
    <%- include ('../partials/_navbar.ejs') %>
    <h1>Edit <%= recipe.name %> recipe</h1>
    <form action="/users/<%= user._id %>/recipes/<%= recipe._id %>?_method=PUT" method="POST">
        <label for="name">Name
            <input type="text" name="name" id="name" required value="<%= recipe.name %>">
        </label>
        <label for="instructions">Instructions
            <textarea type="text" name="instructions" id="instructions"><%= recipe.instructions %></textarea>
        </label>
        <button type="submit">Update</button>
    </form>   
</body>
</html>